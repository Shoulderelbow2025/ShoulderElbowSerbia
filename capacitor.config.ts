import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shoulderelbow.app',
  appName: 'ShoulderElbowSerbia',
  webDir: 'www'
};

export default config;
